# Dashboard server port
DASHBOARD_PORT=4820

# Path to Automaton's state.db
AUTOMATON_DB_PATH=~/.automaton/state.db

# Optional password for dashboard access (leave empty to disable)
DASHBOARD_PASSWORD=
