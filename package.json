{
  "name": "automaton-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter server build && pnpm --filter web build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
